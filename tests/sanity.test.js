import { describe, it, expect } from "vitest";

// A throwaway test that only proves the Vitest setup works. We'll keep it as a smoke test.
describe("test setup", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
